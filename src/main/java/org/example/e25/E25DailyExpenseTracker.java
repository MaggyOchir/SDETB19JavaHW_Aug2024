package org.example.e25;

import java.util.Scanner;

public class E25DailyExpenseTracker {
    public static void main(String[] args) {
        // Create a Scanner object to read input

        // Print prompt for user to input morning expense

        // Capture the morning expense

        // Print prompt for user to input afternoon expense

        // Capture the afternoon expense

        // Print prompt for user to input evening expense

        // Capture the evening expense

        // Calculate the total expense

        // Define a budget

        // Check if the total expense is within the budget

        // Print the total expense

        // Print if the expense is within the budget or over the budget
    }
}
