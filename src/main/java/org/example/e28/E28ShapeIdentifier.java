package org.example.e28;

import java.util.Scanner;

public class E28ShapeIdentifier {
    public static void main(String[] args) {
        // Create a Scanner object to read input

        // Print prompt for user to input length

        // Capture the length

        // Print prompt for user to input width

        // Capture the width

        // Check if length and width are equal and identify the shape

        // If length and width are equal, print "The shape of your object is square"

        // If length and width are not equal, print "The shape of your object is rectangle"
    }
}
